<!DOCTYPE html>
<script src="clientSideFunctionality.js"></script>

<div id = "projectsWrapper">
    <% projectPageData.projects.forEach((project)=>{ %>
         <div>
             <img style ="height:10px"src="<%= project.image_url%>"/>
             <a href = "<%=project.url%>"><%=project.name%></a>
            <%  %> 
             Updated: <%= new Date(project.update*1000).toLocaleString() %>
        </div> 
    <% }) %>
</div>
<button id ="prev" onclick ="prev(<%-projectPageData.page%>)"> Prev </button>

<button id="next" onclick ="next(<%-projectPageData.page%>)">> Next </button>
</html>